# Database Configuration
DATABASE_URL=postgres://postgres:postgres@localhost:5432/configuratix?sslmode=disable

# Server Ports
BACKEND_PORT=8080
FRONTEND_PORT=3000

# JWT Secret (change in production!)
JWT_SECRET=change-me-in-production-use-strong-random-secret

# Frontend API URL (optional, defaults to http://localhost:BACKEND_PORT)
# NEXT_PUBLIC_API_URL=http://localhost:8080
